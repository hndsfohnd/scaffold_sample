<div class="bodytag">
<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field amadeus__comand">
    <%= form.label :問題文 ,class:"amadeus__label"%>
    <%= form.text_field :question ,class:"amadeus"%>
    <%= form.label :答え ,class:"amadeus__label"%>
    <%= form.text_field :answer ,placeholder:'スペースは入れないでください',class:"amadeus"%>
  </div>

  <div class="actions submit__button">
  <%= form.submit type="登録する", class:"submit__new" %>
  
  </div>
  </div>
<% end %>
